import {ITodo} from "../../Imodels/ITodo.ts";

const Todo = ({id, title, checked, description}: ITodo) => {
    return (
        <div className={'w-full'}>

        </div>
    );
};

export default Todo;